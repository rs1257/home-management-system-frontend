// TODO need to create a list of expenses and subscriptions. And when they are due for renewal.
// TODO add fitness and food tracker?

import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/expenses")({
	component: () => <></>,
});
